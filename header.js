var Discord = require("discord.js"),
    lit(format, mWordList(makeWordList)) = require("./libraries/format.js"),
    ai = require("ai"),
    lit(Interface) = require("ai/lib/interface.js"),
    lit(TreeMap) = require("ai/lib/tree"),
    lit(EventEmitter) = require("events"),
    token = require("./../token.js");
defCurried(isNamed, name(v), v.name === name);
var treeMap = create(TreeMap);
var Model = create(ai.Model);
var client = (new Discord.Client());